  <% groupArray.forEach(animal => { %>
    <div>
      <h2><%= animal.family %></h2>
      <h3><%= animal.name</h3> %></h3>
      <img src="<%= animal.image %>" alt="<% animal.name %>">
    </div>
  <%})%>